version: "3"

# Control startup and shutdown order: https://docs.docker.com/compose/startup-order/

services:

  user-agent:
    build:
      context: app
      dockerfile: Dockerfile
#    image: user-agent:latest
#    restart: always
    ports:
      - 5000:5000
    depends_on:
      - "storage_server"

  storage_server:
    build:
      context: storage_server
      dockerfile: Dockerfile
#    image: storage_server:latest
#    restart: always
    ports:
      - 5005:5005
      - 10001:10001
    depends_on:
      - "weather_station"

  weather_station:
    build:
      context: weather_station
      dockerfile: Dockerfile
#    image: weather_station:latest
#    restart: always

